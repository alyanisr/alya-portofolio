import { archiveBeyond, saveActivity, saveOrganization } from "@/app/(admin)/admin/beyond/actions"; import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function BeyondEditor() { const [organizations, activities] = await Promise.all([db.organization.findMany({ orderBy: { startDate: "desc" } }), db.activity.findMany({ orderBy: { createdAt: "desc" } })]); const common = <><input name="status" type="hidden" value="DRAFT" /><input name="visibility" type="hidden" value="PUBLIC" /><input name="sortOrder" type="hidden" value="0" /></>; return <><p className="eyebrow">Beyond the screen</p><h1>People, leadership, and contribution.</h1><p className="admin-intro">Organization and activity content carries the human, collaborative side of the portfolio without turning it into a CV dump.</p><section className="admin-workspace"><h2>Add organization</h2><form action={saveOrganization} className="editor-form compact-form"><label>Name<input name="name" required /></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Role<input name="role" required /></label><label>Start date<input name="startDate" type="date" required /></label><label>End date<input name="endDate" type="date" /></label><label>Current<select name="isCurrent" defaultValue="false"><option value="false">No</option><option value="true">Yes</option></select></label><label>Story<textarea name="description" /></label><label>Responsibilities <small>one per line</small><textarea name="responsibilities" /></label><label>Initiatives <small>one per line</small><textarea name="initiatives" /></label><label>Impact<textarea name="impact" /></label><label>Members<input name="memberCount" type="number" min="0" /></label><label>Departments<input name="departmentCount" type="number" min="0" /></label>{common}<button className="button">Create organization</button></form></section><section className="admin-workspace"><h2>Add activity</h2><form action={saveActivity} className="editor-form compact-form"><label>Title<input name="title" required /></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Organization<select name="organizationId" defaultValue=""><option value="">Independent activity</option>{organizations.filter((x) => !x.deletedAt).map((x) => <option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Role<input name="role" /></label><label>Date<input name="date" type="date" /></label><label>Description<textarea name="description" /></label><label>Outcome<textarea name="outcome" /></label><input name="featured" type="hidden" value="false" />{common}<button className="button">Create activity</button></form></section><section className="admin-workspace"><h2>Published and draft entries</h2><div className="admin-list">{organizations.map((x) => <div className="admin-list-row" key={x.id}><span><b>{x.name}</b><small>{x.role} · {x.status.toLowerCase()}</small></span><form action={archiveBeyond}><input name="type" type="hidden" value="organization" /><input name="id" type="hidden" value={x.id} /><button className="button">Archive</button></form></div>)}{activities.map((x) => <div className="admin-list-row" key={x.id}><span><b>{x.title}</b><small>activity · {x.status.toLowerCase()}</small></span><form action={archiveBeyond}><input name="type" type="hidden" value="activity" /><input name="id" type="hidden" value={x.id} /><button className="button">Archive</button></form></div>)}</div></section></>; }
