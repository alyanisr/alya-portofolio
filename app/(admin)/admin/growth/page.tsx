import { saveLearning } from "@/app/(admin)/admin/growth/actions";
import { db } from "@/lib/db";

export const dynamic = "force-dynamic";
export default async function GrowthPage() { const items = await db.learningItem.findMany({ orderBy: { sortOrder: "asc" } }); return <><p className="eyebrow">Growth content</p><h1>Keep learning honest.</h1><form action={saveLearning} className="editor-form"><label>Title<input name="title" required /></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Category<input name="category" required /></label><label>Maturity<select name="maturity" defaultValue="EXPLORING"><option>CORE</option><option>USED</option><option>FAMILIAR</option><option>EXPLORING</option><option>LEARNING</option></select></label><label>Description<textarea name="description" /></label><label>Why<input name="why" /></label><label>Progress note<input name="progressNote" /></label><label>Resource URL<input name="resourceUrl" type="url" /></label><input type="hidden" name="status" value="DRAFT" /><input type="hidden" name="visibility" value="PUBLIC" /><input type="hidden" name="sortOrder" value="0" /><button className="button">Add learning item</button></form><div className="admin-list">{items.map((item) => <div key={item.id}><b>{item.title}</b><span>{item.maturity.toLowerCase()} · {item.status.toLowerCase()}</span></div>)}</div></>; }
