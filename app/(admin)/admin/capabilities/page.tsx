import { archiveSkill, restoreSkill, saveCategory, saveSkill } from "@/app/(admin)/admin/capabilities/actions";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function CapabilitiesEditor() { const categories = await db.skillCategory.findMany({ where: { deletedAt: null }, include: { skills: { orderBy: [{ sortOrder: "asc" }, { name: "asc" }] } }, orderBy: { sortOrder: "asc" } }); return <><p className="eyebrow">Capabilities</p><h1>Evidence, not percentages.</h1><p className="admin-intro">Capabilities are reusable content. Their maturity and evidence are maintained once, then shown across work, experience, lenses, and public discovery.</p><section className="admin-workspace"><h2>Add category</h2><form action={saveCategory} className="inline-editor"><input name="name" placeholder="Business & Analysis" required /><input name="slug" placeholder="business-analysis" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /><input name="sortOrder" type="number" min="0" defaultValue="0" /><input name="description" placeholder="Optional description" /><button className="button">Add category</button></form></section><section className="admin-workspace"><h2>Add capability</h2><form action={saveSkill} className="editor-form compact-form"><label>Name<input name="name" required /></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Category<select name="categoryId" required defaultValue=""> <option disabled value="">Select category</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Maturity<select name="maturity" defaultValue="FAMILIAR">{["CORE", "USED", "FAMILIAR", "EXPLORING", "LEARNING"].map((value) => <option key={value}>{value}</option>)}</select></label><label>Description<textarea name="description" /></label><label>Evidence note<textarea name="evidence" placeholder="Where or how this capability was used" /></label><input name="visibility" type="hidden" value="PUBLIC" /><input name="featured" type="hidden" value="false" /><input name="sortOrder" type="hidden" value="0" /><button className="button">Create capability</button></form></section>{categories.map((category) => <section className="admin-workspace" key={category.id}><h2>{category.name}</h2><div className="content-editor-list">{category.skills.map((skill) => <div key={skill.id}><form action={saveSkill}><input name="id" type="hidden" value={skill.id} /><input name="name" defaultValue={skill.name} required /><input name="slug" defaultValue={skill.slug} required /><input name="categoryId" type="hidden" value={category.id} /><label>Maturity<select name="maturity" defaultValue={skill.maturity}>{["CORE", "USED", "FAMILIAR", "EXPLORING", "LEARNING"].map((value) => <option key={value}>{value}</option>)}</select></label><label>Visibility<select name="visibility" defaultValue={skill.visibility}>{["PUBLIC", "RESTRICTED", "PRIVATE"].map((value) => <option key={value}>{value}</option>)}</select></label><label>Featured<select name="featured" defaultValue={String(skill.featured)}><option value="false">No</option><option value="true">Yes</option></select></label><input name="sortOrder" type="number" min="0" defaultValue={skill.sortOrder} /><textarea name="description" defaultValue={skill.description ?? ""} aria-label={`${skill.name} description`} /><textarea name="evidence" defaultValue={skill.evidence ?? ""} aria-label={`${skill.name} evidence`} /><button className="button">Save</button></form><form action={skill.deletedAt ? restoreSkill : archiveSkill}><input name="id" type="hidden" value={skill.id} /><button className="button">{skill.deletedAt ? "Restore" : "Archive"}</button></form></div>)}</div></section>)}</>; }
