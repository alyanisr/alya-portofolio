import Link from "next/link"; import { createAchievement } from "@/app/(admin)/admin/achievements/actions";
export default function NewAchievementPage() { return <><p className="eyebrow">New achievement</p><h1>Record the proof.</h1><p className="admin-intro">This begins as a private draft. Add context and evidence before it becomes public.</p><form action={createAchievement} className="editor-form"><label>Title<input name="title" required autoFocus /></label><label>Stable URL slug<input name="slug" required pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Organization<input name="organization" required /></label><label>Category<select name="category" defaultValue="AWARD">{["COMPETITION", "AWARD", "SCHOLARSHIP", "RECOGNITION", "HAKI", "CERTIFICATION"].map((item) => <option key={item}>{item}</option>)}</select></label><label>Date<input name="date" type="date" required /></label><button className="button">Create private draft</button><Link className="text-link" href="/admin/achievements">Cancel</Link></form></>; }
