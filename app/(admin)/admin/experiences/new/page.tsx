import Link from "next/link";
import { createExperience } from "@/app/(admin)/admin/experiences/actions";

export default function NewExperiencePage() { return <><p className="eyebrow">New experience</p><h1>Start with the context.</h1><p className="admin-intro">This creates a private draft. Add evidence and publish only when the story is ready.</p><form action={createExperience} className="editor-form"><label>Company<input name="company" autoFocus required /></label><label>Role<input name="role" required /></label><label>Stable URL slug<input name="slug" required placeholder="company-role" pattern="[a-z0-9]+(-[a-z0-9]+)*" /></label><label>Employment type<select name="employmentType" defaultValue="INTERNSHIP"><option>FULL_TIME</option><option>PART_TIME</option><option>INTERNSHIP</option><option>CONTRACT</option><option>FREELANCE</option><option>VOLUNTEER</option></select></label><label>Start date<input name="startDate" type="date" required /></label><label>Concise summary<textarea name="summary" rows={5} required placeholder="What work, context, and responsibility should this entry explain?" /></label><button className="button">Create private draft</button><Link className="text-link" href="/admin/experiences">Cancel</Link></form></>; }
